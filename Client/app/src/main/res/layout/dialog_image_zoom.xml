<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dialog_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/video_stage"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Zoom image with PhotoView for zoom functionality -->
    <com.github.chrisbanes.photoview.PhotoView
        android:id="@+id/iv_zoom_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:scaleType="centerInside"
        android:adjustViewBounds="true"
        android:contentDescription="@string/zoomed_image_cd" />

    <!-- Close button floating -->
    <ImageView
        android:id="@+id/iv_close"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="@dimen/screen_edge_inset"
        android:layout_marginEnd="@dimen/screen_edge_inset"
        android:background="@drawable/bg_icon_circle_dark"
        android:padding="12dp"
        android:clickable="true"
        android:focusable="true"
        android:src="@drawable/ic_close"
        android:contentDescription="@string/close_cd"
        app:tint="@color/md3_primary" />

</RelativeLayout>